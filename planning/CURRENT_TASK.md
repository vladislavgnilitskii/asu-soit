# CURRENT_TASK.md

## Текущий статус
**Этапы 0, 1, 2, 3 — ВЫПОЛНЕНЫ (2026-07-02).**

## Что сделано в Этапе 3 (Склад)
- Каталог запчастей (`spare_parts` + `part_categories`): CRUD-минимум.
- Приход от поставщика (`POST /spare-parts/:id/receive`) — увеличивает остаток.
- Списание (`POST /spare-parts/:id/writeoff`) — с проверкой остатка, блокировкой
  строки (`FOR UPDATE`) от гонки при параллельных запросах.
- Выдача в ремонт (`POST /requests/:id/parts`) — списывает остаток, пишет
  `stock_movements` + `request_parts`, цена берётся из `sale_price` сервером
  (не от клиента); повторная выдача той же детали на ту же заявку — 409
  (через код ошибки Postgres `23505`, не пред-проверку — гонка).
- Первое использование роли `storekeeper`.
- Проверено: `go build/vet/test` + рантайм-смоук (приход/списание/выдача,
  оба конфликтных сценария — недостаток на складе, повторная выдача); данные
  удалены, baseline восстановлен.

Детали — `STATE.md` (блок «Итог Этапа 3»), решения — `DECISIONS.md` (D-009…D-011,
D-007-warehouse).

## Следующий шаг — Этап 4 «Финансы» (Roadmap, STATE.md §7)
1. `invoices` — выставление счёта по закрытой заявке: сумма из `request_parts`
   (детали) + `final_cost` (работа).
2. Решить: добавлять ли колонку способа оплаты (`payment_method` — enum уже
   создан в БД, но нигде не используется) — это изменение схемы, потребует
   новой миграции и согласования.
3. Статусы счёта: `pending` → `paid`/`cancelled`.

Другие кандидаты (по желанию, не блокируют Этап 4): Employees CRUD, Audit log,
пагинация списков, `GetAll` клиентов с подтипом.

Начинать Этап 4 — по готовности.
