# CURRENT_TASK.md

## Текущий статус
**Этап 0 (Стабилизация) и Этап 1 (Клиенты-организации + Devices) — ВЫПОЛНЕНЫ (2026-07-02).**

## Что сделано в Этапе 1
- Полноценные клиенты-организации: создание (`name/inn/kpp/contact_person`) и
  чтение подтипа через `GET /clients/:id` (`ClientDetails`).
- Модуль Devices: repository + handler + маршруты + DI; RBAC на `POST /devices`
  (admin/manager); справочник `GET /device-types`.
- Миграция 009 применена к живой БД.
- Проверено: `go build/vet/test` + рантайм-смоук (организация + устройство,
  чтение с подтипом); тестовые данные удалены.

Детали — `STATE.md` (блоки «Итог Этапа 0/1»), решения — `DECISIONS.md`.

## Следующие шаги (Roadmap → MVP, STATE.md §7)
Кандидаты на Этап 2 «Заявки end-to-end»:
1. Назначение инженера (`assigned_to`) на заявку.
2. Диагностика и стоимость (`diagnostic_result`, `estimated_cost`, `final_cost`).
3. Закрытие заявки (`closed_at`) + чтение истории статусов (`GET /requests/:id/history`).
4. Валидация существования `status_id`/заявки до смены статуса (STATE.md §5.7).

Мелкий тех-долг на выбор: `GetAll` клиентов с подтипом; обновление/удаление клиента.

Начинать следующий этап — по готовности.
