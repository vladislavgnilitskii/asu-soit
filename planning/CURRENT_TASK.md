# CURRENT_TASK.md

## Текущий статус
**Этапы 0, 1, 2 — ВЫПОЛНЕНЫ (2026-07-02).**

## Что сделано в Этапе 2 (Заявки end-to-end)
- `PATCH /requests/:id/assign` — назначение исполнителя (admin/manager).
- `PATCH /requests/:id` — частичное обновление диагностики/стоимости
  (admin/manager/engineer).
- `PATCH /requests/:id/close` — закрытие: статус `closed`, `closed_at`, история.
- `GET /requests/:id/history` — история статусов (код/название статуса + ФИО).
- Валидация §5.7: несуществующий `status_id` → 400, нет заявки → 404
  (sentinel-ошибки в репозитории).
- Проверено: `go build/vet/test` + рантайм-смоук полного цикла; данные удалены.

Детали — `STATE.md` (блок «Итог Этапа 2»), решения — `DECISIONS.md` (D-007, D-008).

## Следующий шаг — Этап 3 «Склад» (Roadmap, STATE.md §7)
1. Запчасти: repository/handler для `spare_parts` (+ `part_categories`).
2. Движение склада `stock_movements` (приход/выдача/списание) с изменением
   `quantity_in_stock` в транзакции.
3. Привязка запчастей к заявке (`request_parts`) — списание в ремонт.

Открытые мелочи (по желанию): `GetAll` клиентов с подтипом; обновление/удаление
клиента; ограничение `assigned_to` ролью engineer.

Начинать Этап 3 — по готовности.
