# CURRENT_TASK.md

## Текущий статус
**Этапы 0–5 — ВЫПОЛНЕНЫ.** (Этап 5 — 2026-07-08.)

## Что сделано в Этапе 5 (Управление сотрудниками)
- `POST /employees` — создание с bcrypt-хешированием пароля (в БД только хеш).
- `GET /employees`, `GET /employees/:id` — список/карточка без секретов.
- `PATCH /employees/:id` — обновление профиля/роли; деактивация вместо удаления.
- Вся группа — только `admin`.
- Проверки: дубль логина → 409, несуществующая роль → 400, короткий пароль → 400.
- Проверено: `go build/vet/test` + рантайм-смоук (вкл. вход созданным сотрудником,
  хеш в БД, блокировку деактивированного, RBAC 403); данные удалены.

Детали — `STATE.md` (блок «Итог Этапа 5»), решения — `DECISIONS.md` (D-015…D-017).

## Что осталось до «полного» бэкенда (STATE.md §3)
Все функциональные модули + управление сотрудниками — готовы. Осталась обвязка:
1. **Audit log** — таблица есть, запись не ведётся. Требует обсуждения архитектуры
   (middleware vs SQL-триггеры) — **новое**, разберём вместе.
2. **Способ оплаты** (`payment_method`) — колонка в `invoices` + миграция.
3. **Полировка**: пагинация списков (`GetAll` без limit/offset); тесты
   repository/handler (сейчас покрыт только `auth`); смена пароля сотрудника.

Порядок — по согласованию. Audit log — самый содержательный кандидат.
