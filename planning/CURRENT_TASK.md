# CURRENT_TASK.md

## Текущий статус
**Этапы 0, 1, 2, 3, 4 — ВЫПОЛНЕНЫ.** (Этап 4 — 2026-07-08.)

## Что сделано в Этапе 4 (Финансы)
- `POST /requests/:id/invoice` — счёт по закрытой заявке; сумма считается на
  сервере: `final_cost` (работа) + `SUM(request_parts)` (детали).
- `GET /requests/:id/invoice`, `GET /invoices/:id` — чтение счёта.
- `PATCH /invoices/:id/status` — оплата/отмена (`pending` → `paid`/`cancelled`).
- Проверки: незакрытая заявка → 409, повторный счёт → 409 (UNIQUE + код 23505),
  повторная оплата → 409, невалидный статус → 400, нет счёта → 404.
- Первое использование роли `accountant`.
- Часть кода написана пользователем в учебных целях (domain, sentinel-ошибки,
  GetByID, хендлеры); транзакционная логика — сгенерирована.
- Проверено: `go build/vet/test` + полный рантайм-смоук; данные удалены.

Детали — `STATE.md` (блок «Итог Этапа 4»), решения — `DECISIONS.md`
(D-012…D-014, D-007-finance).

## Следующие шаги (что осталось до полного бэкенда, STATE.md §3)
Функциональные модули из БД — **закрыты все** (клиенты, устройства, заявки,
склад, финансы). Осталась «обвязка»:
1. **Employees CRUD** — завести/отключить сотрудника (сейчас только логин).
2. **Audit log** — таблица есть, запись не ведётся; решить архитектуру
   (middleware vs SQL-триггеры) — требует обсуждения.
3. **Способ оплаты** (`payment_method`) — колонка в `invoices` + миграция.
4. **Полировка**: пагинация списков (`GetAll` без limit/offset), тесты
   repository/handler (сейчас покрыт только `auth`).

Порядок и объём — по согласованию.
