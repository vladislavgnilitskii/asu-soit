# CURRENT_TASK.md

## Текущая задача
**Этап 0 — Стабилизация и закрытие security-дыр — ВЫПОЛНЕН (2026-07-02).**

## Цель
Закрыть все известные дыры безопасности и довести существующий код до чистого
состояния, не добавляя нового функционала, — чтобы дальнейшая разработка модулей
(Devices/Warehouse/Finance) велась на надёжном фундаменте.

## Что сделано
- Безопасность: автор статуса из JWT (не из заголовка); RBAC на clients/requests;
  обобщённые 500 без утечки деталей; fail-fast на дефолтные секреты в production;
  отказ по организациям вместо порчи данных.
- Полировка: `clinet.go`→`client.go` + единые хелперы; 404 vs 500; валидация ФИО;
  nullable-поля через указатели; `gen_crypto_pass.go`→`cmd/genhash` без хардкода.
- Тесты: пакет `auth` (JWT + middleware), `go test ./...` зелёный.
- БД: подготовлена миграция 009 (индексы) — **не применена**.
- Проверено: `go build/vet/test` + рантайм-смоук против живой БД (данные восстановлены).

Детали — в `STATE.md` (блок «Итог Этапа 0»), решения — в `DECISIONS.md`.

## Следующий шаг (требует решения пользователя)
1. **Применить миграцию 009?** (удаление дубль-индексов + индексы на FK.) Готова,
   но не применена — это изменение схемы.
2. **Выбрать следующий модуль** (Roadmap, STATE.md §7):
   - Этап 1 — Клиенты (организации) + Devices CRUD, ИЛИ
   - сначала добить мелочи (валидация `status_id`/существования заявки — §5.7).

Реализацию следующего этапа не начинать без подтверждения.
